export const metadata = {
  title: 'Pricing | React GTM Kit Next.js example'
};

export default function PricingPage() {
  return (
    <section className="content">
      <h2>Pricing</h2>
      <p>
        GTM Kit keeps pricing simple: the library is MIT licensed and free to use. The focus is on predictable
        integration patterns so teams can launch analytics without guesswork.
      </p>
      <p>
        Use this page to observe how client-side transitions maintain the data layer queue and emit a new
        <code>page_view</code> event.
      </p>
    </section>
  );
}
